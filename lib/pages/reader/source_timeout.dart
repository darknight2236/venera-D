// Re-exported so reader code keeps its existing import path; the canonical
// implementation lives in foundation (also used by image providers).
export 'package:venera/foundation/source_request.dart';
